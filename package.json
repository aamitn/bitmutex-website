{
  "name": "bitmutex-strapi-next-app",
  "version": "1.0.0",
  "description": "Strapi5 NextJs15 App built to use as website for Bitmutex Technologies",
  "type": "module",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "pnpm dev --prefix ../client/",
    "server": "pnpm dev --prefix ../server/",
    "setup:client": "cd client && pnpm install && node --import tsx ../copy-env.mts ./",
    "setup:server": "cd server && pnpm install && node --import tsx ../copy-env.mts ./",
    "setup:root": "pnpm install && node --import tsx ./copy-env.mts ./",
    "spawn": "pnpm setup:root && pnpm setup:client && pnpm setup:server",
    "dev": "pnpm concurrently \"cd server && pnpm develop\" \"npx wait-on http://localhost:1337 && cd client && pnpm dev\"",
    "build": "pnpm concurrently --kill-others \"cd server && pnpm build && pnpm start\" \"npx wait-on http://localhost:1337 && cd client && pnpm build && echo 'Build Successful!' && exit 0 || echo ' Build Failed!' && exit 1\"",
    "start": "pnpm concurrently \"cd server && pnpm start\" \"npx wait-on http://localhost:1337 && cd client && pnpm start\"",
    "seed": "cd server && pnpm strapi import -f ../seed-data.tar.gz --force",
    "export": "cd server && pnpm strapi export --no-encrypt -f ../seed-data",
    "repo:upstream": "git fetch upstream && git merge upstream/main",
    "repo:sync": "git pull origin main",
    "backup": "pnpm run export && node --import tsx ./r2-backup.mts",
	"clean": "node --import tsx ./scripts/clean.ts",
    "deepclean": "node --import tsx ./scripts/clean.ts --locks"
  },
  "dependencies": {
    "@types/node": "^24.2.0",
    "bitmutex-strapi-next-app": "file:",
    "concurrently": "^9.2.0",
    "dotenv": "^17.2.1",
    "typescript": "^5.9.2",
    "wait-on": "^8.0.4"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.863.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3"
  },
    "pnpm": {
    "overrides": {
      "@strapi/design-system": ">=2.2.0",
      "@strapi/sdk-plugin": ">=6.1.0"
    }
  },
  "overrides": {
    "@strapi/design-system": ">=2.2.0",
    "@strapi/sdk-plugin": ">=6.1.0"
  },
  
  "license": "Apache-2.0",
  "trustedDependencies": [
    "@tsparticles/engine",
    "better-sqlite3",
    "esbuild",
    "sharp",
    "unrs-resolver"
  ]
}